---
apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: "{{ namespace }}"
  labels:
    app: mysql
spec:
  type: ClusterIP
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
  selector:
    app: mysql
...
